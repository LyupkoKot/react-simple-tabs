import React from "react";
import styled, { css } from "styled-components";

const TableHeaderStyle = styled.li`
  color: rgba(0, 0, 0, 0.54);
  font-size: 0.85rem;
  font-weight: 800;
`;

export default TableHeaderStyle;
